#include <iostream>   //Задача 8
using namespace std;

void gaussJordan(double matrix[3][4]) {
    int n = 3;
    for (int i = 0; i < n; i++) {
        double pivot = matrix[i][i];
        for (int j = 0; j <= n; j++) {
            matrix[i][j] /= pivot;
        }
        for (int k = 0; k < n; k++) {
            if (k != i) {
                double factor = matrix[k][i];
                for (int j = 0; j <= n; j++) {
                    matrix[k][j] -= factor * matrix[i][j];
                }
            }
        }
    }
}

int main() {
    double matrix[3][4] = {
        {3, -1, 2, 5},
        {2, 4, -1, 6},
        {1, -3, 1, 4}
    };

    gaussJordan(matrix);

    for (int i = 0; i < 3; i++) {
        cout << "x" << i + 1 << " = " << matrix[i][3] << endl;
    }

    return 0;
}
